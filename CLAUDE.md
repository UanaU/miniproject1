# 아파트 관리비 조회 시스템 - 프로젝트 요구사항 (최종)

## 프로젝트 개요
- **기간**: 3일
- **목표**: 같은 아파트 단지 입주민이 로그인 후 본인 세대 관리비를 조회하고, 같은 평수 평균 / 같은 평수+가구원수 평균 / 전월 / 작년 동월과 비교하며, AI 기반 증가 원인 분석을 받아보는 웹 서비스
- **비교 범위**: 같은 단지 내로 한정 (다중 단지 비교 기능 없음)
- **팀 구성**: 데이터 시각화·조회·비교 파트 (3인) / LLM 원인분석 파트 (2인)

## 기술 스택

| 영역 | 기술 |
|---|---|
| 로직 | Python |
| 백엔드 | Django + Django REST Framework |
| 프론트엔드 | HTML, CSS, React |
| 그래프 | Recharts (React) |
| DB | MySQL |
| 인증 | Django 세션 인증 |
| 연동 | django-cors-headers, requests, python-dotenv |
| AI 원인분석 | 실제 LLM API 연동 (Google Gemini API) |
| 더미데이터 | Python + Faker (ko_KR) |

## DB 스키마 (2개 테이블, 최종 확정)

### 1. 회원정보DB
| 필드명 | 타입 | 비고 |
|---|---|---|
| 회원_index | int | PK |
| 회원id | varchar | 로그인용 |
| 회원비밀번호 | varchar | |
| 이름 | varchar | |
| 연락처(휴대폰) | varchar | |
| 아파트동 | int | |
| 아파트호수 | int | |
| 아파트평수 | int | |
| 가구원수 | int | |

### 2. 관리비DB
| 필드명 | 타입 | 비고 |
|---|---|---|
| 관리비_index | int | PK |
| 회원_index | int | FK → 회원정보DB |
| 청구년월 | YYYYMMDD | sysdate 형식, 해당월 1일 기준 |
| 납부상태 | bool | 완납=True, 미납=False |
| 합계금액 | int | 세대별+공동+동공통 합산 |
| **[세대별]** 전기세 | int | 세대마다 다름 |
| 수도세 | int | 세대마다 다름 |
| 가스비 | int | 세대마다 다름 |
| **[단지 공통]** 일반관리비 | int | 같은 단지 전체 동일 |
| 공동전기료 | int | 같은 단지 전체 동일 |
| 청소비 | int | 같은 단지 전체 동일 |
| 경비비 | int | 같은 단지 전체 동일 |
| **[동 공통]** 승강기전기료 | int | 같은 동끼리만 동일, 동마다 기준값 다름 |
| 수선유지비 | int | 같은 동끼리만 동일, 동마다 기준값 다름 |

> 공지사항DB, 문의DB, 소통창구, 다중단지 비교, 공동관리비 별도 월별 DB는 전부 스코프에서 제외 확정.

### 관리비 항목 산정 원칙 (중요)
관리비 항목은 성격이 다른 세 그룹으로 나뉘며, 더미데이터·실제 계산 로직 모두 이 원칙을 따라야 함.

1. **세대별 항목** (전기세/수도세/가스비): 평수 + 가구원수에 따라 세대마다 값이 다름
2. **단지 공통 항목** (일반관리비/공동전기료/청소비/경비비): 같은 청구년월이면 단지 내 모든 세대가 동일한 값 (평수·가구원수 무관)
3. **동 공통 항목** (승강기전기료/수선유지비): 같은 동에 속한 세대끼리는 동일하지만, 동마다 기준값이 다름 (동별 엘리베이터 대수·건물 노후도 차이를 반영)

## 기능 목록 및 우선순위

### 필수 (1~2일차)
1. 로그인 / 로그아웃
2. 마이페이지 (동/호수/평수/가구원수 등 기본정보 표시)
3. 관리비 조회 화면 (세대별/단지공통/동공통 항목 구분 표시)
4. 그래프 시각화 / 비교
   - 전월 대비
   - 같은 평수 평균 대비
   - **같은 평수 + 가구원수 대비**: 평수만으로는 안 잡히는 사용량 차이 보정. 세대별 항목(전기세/수도세/가스비)에서 특히 의미 있는 비교
   - 작년 동월 대비
5. AI 원인분석 (실제 LLM API 연동) — 관리비 비교 화면에서 "AI 분석" 버튼으로 수행. 전월 대비 / 같은 평수(+가구원수) 평균 대비 증가 원인 분석과 절약팁을 한 번의 API 호출로 함께 제공. **상세 설계는 아래 "AI 원인분석 기능 상세 설계" 참고**

## API 계약 (데이터팀 ↔ LLM팀 공유 스펙)

데이터팀이 아래 형식으로 응답하는 API를 만들고, LLM팀은 실제 DB 완성 전에 이 형식의 더미 JSON으로 먼저 프롬프트/연동 작업을 시작한다 (병렬 진행 목적).

```
GET /api/managementfee/summary?year_month=20260901
```

```json
{
  "이번달": {
    "청구년월": "20260901",
    "합계금액": 187000,
    "세대별": {"전기세": 45000, "수도세": 12000, "가스비": 30000},
    "단지공통": {"일반관리비": 40000, "공동전기료": 15000, "청소비": 20000, "경비비": 12000},
    "동공통": {"승강기전기료": 8000, "수선유지비": 5000}
  },
  "전월": { "청구년월": "20260801", "합계금액": 165000, "세대별": {}, "단지공통": {}, "동공통": {} },
  "같은평수평균": { "합계금액": 172000, "세대별": {}, "단지공통": {}, "동공통": {} },
  "같은평수_가구원수평균": {
    "기준": {"평수": 30, "가구원수": 3},
    "합계금액": 176000, "세대별": {}, "단지공통": {}, "동공통": {}
  },
  "작년동월": { "청구년월": "20250901", "합계금액": 158000, "세대별": {}, "단지공통": {}, "동공통": {} }
}
```

> 비교 데이터가 없는 경우(예: 신규입주 1개월차의 전월/작년동월) 해당 키는 `null`로 응답한다. AI 원인분석 API도 이 규칙을 그대로 따른다.

## AI 원인분석 기능 상세 설계

관리비 비교 화면에서 LLM을 이용해 "전월 대비" 및 "같은 평수(+가구원수) 평균 대비" 관리비 증가 원인을 분석하고, 절약팁을 함께 제시하는 기능의 상세 스펙이다. LLM팀은 이 문서를 기준으로 데이터팀의 요약 API(`/api/managementfee/summary`) 완성 전이라도 더미 JSON으로 프롬프트/연동 작업을 먼저 시작할 수 있다.

### 목표
- 이번달 관리비를 전월, 같은 평수 평균, 같은 평수+가구원수 평균과 비교해 어떤 항목이 왜 늘었는지 LLM이 설명
- 원인분석 + 절약팁을 한 번의 API 호출로 함께 받아 호출 횟수 최소화 (기존 요구사항 원칙 유지)
- API 실패 시에도 데모가 끊기지 않도록 폴백 문구 준비

### 화면 흐름
1. 사용자가 관리비 조회/비교 화면 진입 → 기존 요약 API로 비교 데이터를 받아 그래프/표로 표시
2. 화면 내 "AI 분석" 버튼 클릭 → 프론트엔드는 이미 받아둔 요약 데이터를 그대로 실어 AI 분석 전용 API를 호출 (요약 API를 다시 호출하지 않고 캐시된 응답을 재사용해 불필요한 DB 조회 방지)
3. 로딩 상태 표시(LLM 응답까지 수 초 소요 예상) → 완료되면 "요약 한 줄 + 항목별 원인 + 절약팁" 카드로 렌더링
4. 비교 데이터가 없는 세대(신규입주 1개월차 등)는 버튼을 비활성화하고 "비교 데이터가 없어 분석할 수 없습니다" 안내로 대체

### 백엔드 API 스펙 (신규)
```
POST /api/managementfee/ai-analysis
```

요청 바디 — 요약 API 응답을 그대로 재사용 (비교 불가 항목은 `null`):
```json
{
  "이번달": { "청구년월": "20260901", "합계금액": 187000,
    "세대별": {"전기세": 45000, "수도세": 12000, "가스비": 30000},
    "단지공통": {"일반관리비": 40000, "공동전기료": 15000, "청소비": 20000, "경비비": 12000},
    "동공통": {"승강기전기료": 8000, "수선유지비": 5000} },
  "전월": { "청구년월": "20260801", "합계금액": 165000, "세대별": {"전기세": 30000, "수도세": 12000, "가스비": 28000}, "단지공통": {}, "동공통": {} },
  "같은평수평균": { "합계금액": 172000, "세대별": {"전기세": 38000, "수도세": 11500, "가스비": 29000}, "단지공통": {}, "동공통": {} },
  "같은평수_가구원수평균": { "기준": {"평수": 30, "가구원수": 3}, "합계금액": 176000, "세대별": {}, "단지공통": {}, "동공통": {} }
}
```

응답 (성공):
```json
{
  "분석결과": {
    "요약": "이번 달 관리비는 전월 대비 22,000원(13.3%) 증가했습니다. 주요 원인은 전기세입니다.",
    "항목별원인": [
      {
        "항목": "전기세",
        "비교기준": "전월",
        "증감액": 15000,
        "증감률": 50.0,
        "설명": "전월 대비 전기세가 15,000원 늘었고, 같은 평수 평균보다도 7,000원 높습니다."
      }
    ],
    "절약팁": [
      "냉방기기 사용 시간을 하루 1시간만 줄여도 전기세를 약 10% 절감할 수 있습니다.",
      "대기전력 차단 멀티탭을 사용하면 사용 습관 변화 없이 전기 사용량을 줄일 수 있습니다."
    ]
  },
  "생성시각": "2026-09-08T10:00:00+09:00",
  "폴백여부": false
}
```

응답 (실패/타임아웃 — 폴백):
```json
{
  "분석결과": null,
  "폴백여부": true,
  "폴백메시지": "지금은 AI 분석을 불러올 수 없습니다. 잠시 후 다시 시도해주세요."
}
```

### 프롬프트 설계
- **시스템 프롬프트**: 역할(아파트 관리비 분석 도우미), 톤(간결한 존댓말), 출력 형식(자연어 문장 없이 JSON 스키마만 반환)을 고정 지시
- **유저 프롬프트 구성 데이터**: 이번달/전월/같은평수평균/같은평수+가구원수평균의 항목별 금액. 값이 `null`인 비교 기준은 프롬프트에서 아예 제외 (없는 데이터를 있는 것처럼 설명하지 않도록)
- **항목 구분 지시**: 세대별 항목(전기세/수도세/가스비)은 "생활 습관성 절약팁" 대상으로, 단지공통/동공통 항목은 사용자 행동과 무관하므로 "단지 전체 요금 인상" 등으로만 설명하고 절약팁 대상에서 제외하도록 지시
- **증감 판단 임계값**: 항목별 증감률이 낮은 경우(예: 5% 미만)에는 "특별한 증가 요인 없음"으로 요약하고 일반적인 절약팁만 제공하도록 분기
- **출력 형식 강제**: JSON 스키마(위 응답 예시의 `분석결과` 구조)만 반환하도록 지시해 파싱 실패 가능성을 줄임

### Google Gemini API 연동
- (Anthropic API 키 미보유로 Gemini로 전환 — 2026-09-08) `google-genai` SDK 사용
- 모델: 응답속도·비용 대비 이 수준의 분석에는 충분한 Flash 계열을 기본값으로 사용, 필요 시 상위 모델로 상향 가능하도록 모델명은 코드에 하드코딩하지 않고 환경변수로 분리
- `.env` 변수: `GEMINI_API_KEY`, `GEMINI_MODEL` (기존 "LLM API 키는 .env로 분리, 하드코딩 금지" 원칙과 동일하게 적용)
- 출력 형식 강제는 프롬프트 지시에 더해 Gemini의 `response_mime_type="application/json"` 설정으로 이중 보장
- 타임아웃: 예) 10초(`HttpOptions(timeout=10000)`, 단위는 밀리초). 초과 시 폴백 응답으로 전환
- 응답이 JSON 스키마와 불일치해 파싱 실패할 경우에도 폴백 응답으로 전환하고 서버 로그에 원본 응답을 남겨 디버깅에 활용

### 예외처리
| 상황 | 처리 |
|---|---|
| 신규입주(비교 데이터 없음) | AI 분석 버튼 비활성화 + "비교 데이터가 없어 분석할 수 없습니다" 안내 |
| LLM API 호출 실패/타임아웃 | 사전 정의된 폴백 문구 반환 (데모 중 오류 대비 원칙 유지) |
| 응답 JSON 파싱 실패 | 폴백 문구 반환 + 서버 로그 기록 |
| 항목별 증감이 미미함(5% 미만) | "특별한 증가 요인 없음" 요약 + 일반 절약팁만 제공 |

### 테스트 시나리오
- 더미데이터의 전기세 급증 이상치(전체 세대의 약 3%) 계정으로 로그인 → 분석 결과에 "전기세" 항목이 실제로 원인으로 잡히는지 확인
- 신규입주(1개월차) 계정으로 로그인 → AI 분석 버튼 비활성화/안내 문구 확인
- API 키를 임시로 제거하거나 응답을 지연시켜 폴백 문구가 정상 노출되는지 확인
- 미납 상태(약 4%) 계정에서도 분석 기능이 정상 동작하는지 확인 (납부상태와 무관하게 동작해야 함)

## 3일 타임라인

**1일차 — 기반**
- DB 2개 테이블 생성 + 더미 데이터 삽입 (Faker 생성 프로그램 활용)
- 로그인/로그아웃
- 마이페이지
- API 계약(JSON 스펙) 팀 간 확정 → 이후 두 파트 병렬 진행

**2일차 — 핵심 기능**
- 관리비 조회 화면 + 그래프 시각화 (전월/평수평균/평수+가구원수평균/작년동월)
- LLM API 연동 (원인분석 + 절약팁) — 가장 시간 소요 큰 부분, 반드시 이날 완료

**3일차 — 마무리**
- 버그 수정, 화면 다듬기
- 발표자료/데모 준비

## 개발 시 유의사항
- React(포트 3000)와 Django(포트 8000) 분리 구조 → django-cors-headers 설정 필수
- LLM API 키는 .env 파일로 분리, 코드에 하드코딩 금지
- MySQL 커넥터(mysqlclient/PyMySQL) 설치 이슈는 1일차 초반에 미리 확인
- 1개월만 거주한 신규 입주 세대는 전월/작년 대비 비교 데이터가 없음 → "비교 데이터 없음" 등 예외처리 필요 (아래 더미데이터에 의도적으로 다수 포함되어 있으니 이 케이스로 테스트할 것)
- AI 원인분석 API(`/api/managementfee/ai-analysis`)는 요약 API 응답을 재사용하는 구조이므로, 두 API의 JSON 필드명이 어긋나지 않도록 데이터팀·LLM팀이 공유 스키마를 함께 관리할 것

## 더미데이터

### 생성 프로그램
`generate_dummy_data.py` — Python + Faker(ko_KR) 기반. 이름/연락처는 Faker로 생성하고, 관리비 수치는 위 "관리비 항목 산정 원칙"을 그대로 구현한 로직으로 계산.

```bash
pip install faker --break-system-packages
python3 generate_dummy_data.py
```

스크립트 상단 `CONFIG` 영역의 값만 바꾸면 규모/분포 조절 가능 (회원 수, 관리비 레코드 수, 평수 종류, 동 개수, 이상치 비율 등). `SEED` 고정으로 재실행해도 동일한 데이터 재현 가능 (`SEED = None`으로 바꾸면 매번 다른 데이터).

### 생성 규모 및 특징
- **회원**: 1,000명
- **관리비 레코드**: 10,000건
- **평수**: 18 / 24 / 32 / 38 / 45 / 52평 6종류, 같은 평수 안에서도 가구원수(1~6명)를 정규분포로 분산
- **동**: 15개동 (101동~115동)

**거주기간 다양화** (관리비 레코드 보유 개월 수로 표현 — DB에 별도 입주일자 필드는 없음)
| 구간 | 의미 |
|---|---|
| 1개월 | 신규 입주, 비교 데이터 없음 (예외처리 테스트용) |
| 2~9개월 | 단기~중기 거주 |
| 10~23개월 | 1~2년차 |
| 24~35개월 | 2~3년차 |
| 36~48개월 | 3~4년 장기거주, 여러 해에 걸친 추세 데이터 보유 |

평균 거주기간 약 12개월, 최소 1개월~최대 48개월.

**테스트용 이상치**
- 전체 세대의 약 3%는 보유 기간 중 임의 1개월에 전기세가 60~120% 급증 → AI 원인분석 기능이 실제로 원인을 잡아내는지 검증용
- 약 4%는 무작위로 미납 상태 → 납부상태 필드 테스트용

**공동 항목 검증 로직 포함**: 같은 청구년월 기준으로 단지 공통 항목(일반관리비/공동전기료/청소비/경비비)이 전 세대 동일한지, 동 공통 항목(승강기전기료/수선유지비)이 같은 동끼리만 동일한지 스크립트 실행 시 자동 검증 출력.

### 데이터 보존 원칙
- 생성된 CSV와 로그인 테스트 계정(id: user0001~user1000, 비밀번호: pw10001~pw11000)은 삭제하지 말고 그대로 보관
- 생성 프로그램 자체도 프로젝트 저장소에 포함 (발표/데모 전 DB 초기화 후 재실행 가능하도록)
</content>
